# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'podmark' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  pod 'SQLite.swift', '~> 0.12.0'
  pod 'Alamofire', '~> 5.2'
  pod 'URLImage'

end

target 'podmarkshare' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  pod 'SQLite.swift', '~> 0.12.0'
  pod 'Alamofire', '~> 5.2'
  pod 'URLImage'

  # Pods for podmarkshare2

end

target 'add' do
  use_frameworks!

  pod 'SQLite.swift', '~> 0.12.0'
  pod 'Alamofire', '~> 5.2'
  pod 'URLImage'
end
